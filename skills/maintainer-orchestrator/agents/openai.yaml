interface:
  display_name: "Maintainer Orchestrator"
  short_description: "Coordinate one Codex app thread per project"
  default_prompt: "Use $maintainer-orchestrator to coordinate one Codex app thread per project, keep skill maintenance in the root session, keep collaboration subagents read-only and support-only, protect forgotten work, serialize exact-head landing, capture durable decisions, audit dependencies, and propose verified releases."
